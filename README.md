-----------------------INSTALL------------------------------------

pip install .

----------------------HOW TO USE----------------------------------

snapshot -t (select time(default 300s)) -l (select log type: txt or json(default txt))

-----------------------UNINSTALL----------------------------------

pip uninstall snapshot